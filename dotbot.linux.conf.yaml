# linux/mac
- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    # vim 配置
    ~/.vimrc: editor/vim/.vimrc
    # idea 配置
    ~/.ideavimrc: editor/idea/.ideavimrc
    # zshrc 配置
    ~/.zshrc: shell/zshrc/.zshrc
    ~/.p10k.zsh: shell/zshrc/.p10k.zsh
    # linux / macos 专属
    ~/.config/nvim: editor/neovim
    # uv 配置
    ~/.config/uv/uv.toml: lang/python/uv.toml
    # Rime 配置
    ~/Library/Rime/default.custom.yaml: rime-custom/default.custom.yaml
    ~/Library/Rime/squirrel.custom.yaml: rime-custom/squirrel.custom.yaml
    ~/Library/Rime/custom_phrase.txt: rime-custom/custom_phrase.txt
    ~/Library/Rime:
        glob: true
        path: rime-ice/*
        exclude:
            - .git*
            - custom_phrase.txt
            - .*custom.yaml


# - shell:
#   - [git submodule update --init --recursive, Installing submodules]