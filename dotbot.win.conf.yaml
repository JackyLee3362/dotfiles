# windows 专属
- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.vimrc: vim/.vimrc
    ${LOCALAPPDATA}/nvim:
      path: neovim
    # uv 配置
    ${APPDATA}/uv/uv.toml:
      create: true
      path: uv/uv.toml
    # Rime 配置
    ${APPDATA}/Rime:
      # $env:APPDATA
      glob: true
      path: rime-ice/* # rime-japanese/*
      exclude:
        - .git*
        - rime-ice/custom_phrase.txt
        - .*custom.yaml
    ${APPDATA}/Rime/default.custom.yaml:
      relink: true
      path: rime-custom/default.custom.yaml
    ${APPDATA}/Rime/weasel.custom.yaml:
      path: rime-custom/weasel.custom.yaml
    ${APPDATA}/Rime/custom_phrase.txt:
      path: rime-custom/custom_phrase.txt
